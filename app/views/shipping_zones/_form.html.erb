<%= form_for(shipping_zone) do |f| %>
  <% if shipping_zone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shipping_zone.errors.count, "error") %> prohibited this shipping_zone from being saved:</h2>

      <ul>
      <% shipping_zone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :disabled %>
    <%= f.check_box :disabled %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
