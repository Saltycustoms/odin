<%= form_for(shipping_zone_location) do |f| %>
  <% if shipping_zone_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shipping_zone_location.errors.count, "error") %> prohibited this shipping_zone_location from being saved:</h2>

      <ul>
      <% shipping_zone_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :country_code %>
    <%= f.text_field :country_code %>
  </div>

  <div class="field">
    <%= f.label :state %>
    <%= f.text_field :state %>
  </div>

  <div class="field">
    <%= f.label :shipping_zone_id %>
    <%= f.number_field :shipping_zone_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
