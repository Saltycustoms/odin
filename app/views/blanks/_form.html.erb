<%= form_for(blank) do |f| %>
  <% if blank.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blank.errors.count, "error") %> prohibited this blank from being saved:</h2>

      <ul>
      <% blank.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :price_cents %>
    <%= f.number_field :price_cents %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :meta %>
    <%= f.text_field :meta %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
