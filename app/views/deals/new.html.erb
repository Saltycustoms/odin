<% content_for :header_actions do %>
  <%= link_to 'Back', deals_path %>
<% end %>

<%= render "shared/page_header", {title: "New deal for #{@pic.name} (#{@pic.belongable.organization.name} - #{@pic.belongable.name})"} %>

<%= render 'form', deal: @deal, pic: @pic %>

<%= link_to 'Back', @pic.belongable.organization %>
